version: '3.8'

services:
  db:
    image: mariadb:11.4@sha256:70cc072b29b4a89ae07abb2d4da2c64678a7f2dfe092751bb51c87d67dc1338b
    container_name: portal_db
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: portal_db
      MYSQL_USER: portal_user
      MYSQL_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--su-mysql", "--connect", "--innodb_initialized"]
      interval: "15s"
      timeout: 5s
      retries: 10
    networks:
      - portal_internal

  search:
    image: getmeili/meilisearch:v1.48.3@sha256:c1a52f17c759c2cd6349eede3d5108b8dac07b97e10665b1d64a2d4961c2fd29
    container_name: portal_search
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
    environment:
      MEILI_MASTER_KEY: ${MEILI_MASTER_KEY}
      MEILI_ENV: production
    volumes:
      - search_data:/meili_data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7700/health"]
      interval: "15s"
      timeout: 5s
      retries: 5
    networks:
      - portal_internal

  composer_init:
    image: composer:2.10.3@sha256:a5f59b9fd2faf31218632be4809dc6491761085e8064c31dc3b84378c48c248b
    user: "1000:1000"
    container_name: portal_composer
    volumes:
      - /home/webadmin/websites/api-portal.reisinger.pictures:/app:Z
    command: install --ignore-platform-reqs --no-interaction --optimize-autoloader

  backend:
    image: ghcr.io/reisi007/portal-base:8.5@sha256:484410448bdff80c2571778e69e3ec264a0033baf22b0534eaac12331f6940ba
    user: "1000:1000"
    container_name: portal_backend
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      search:
        condition: service_healthy
      composer_init:
        condition: service_completed_successfully
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
    volumes:
      - /home/webadmin/websites/api-portal.reisinger.pictures:/var/www/html:Z
      - /home/webadmin/portal/images:/var/www/photos:Z
      - /home/webadmin/websites/ftp:/var/www/ftp:Z
    environment:
      - DB_HOST=db
      - DB_DATABASE=portal_db
      - DB_USERNAME=portal_user
      - DB_PASSWORD=${DB_PASSWORD}
      - APP_ENV=${APP_ENV}
      # Default false: never enable debug by accident in production.
      - APP_DEBUG=${APP_DEBUG:-false}
      - APP_URL=${APP_URL}
      - FRONTEND_URL=${FRONTEND_URL}
      - APP_KEY=${APP_KEY}
      - JWT_SECRET=${JWT_SECRET}
      # Separater Key für die verschlüsselte Ablage sensibler Model-Dateien
      # (Altersnachweis/Fotos) + answers-Snapshots. Rotation via
      # FILE_ENCRYPTION_PREVIOUS_KEYS.
      - FILE_ENCRYPTION_KEY=${FILE_ENCRYPTION_KEY}
      - FILE_ENCRYPTION_PREVIOUS_KEYS=${FILE_ENCRYPTION_PREVIOUS_KEYS}
      - LOG_CHANNEL=stderr
      - ADMIN_EMAIL=${ADMIN_EMAIL}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
      - ACCOUNTING_EMAIL=${ACCOUNTING_EMAIL}
      # ---
      # E-Mail-Versand (SMTP via ZeptoMail, Migration vom Gmail-REST-OAuth am 2026-08-24)
      # Konfiguration liegt in .env.production:
      #   MAIL_MAILER=smtp
      #   MAIL_HOST=smtp.zeptomail.eu
      #   MAIL_PORT=587
      #   MAIL_ENCRYPTION=tls
      #   MAIL_USERNAME=emailapikey
      #   MAIL_PASSWORD=<ZeptoMail-API-Key>
      #   MAIL_FROM_ADDRESS=noreply@reisinger.pictures
      # Der Gmail-REST-OAuth-Transport (GmailRestTransport) bleibt als Fallback
      # im Code erhalten; OAUTH_*-Secrets sind aus dem PROD-Setup entfernt.
      # ---
      - MAIL_MAILER=${MAIL_MAILER}
      - MAIL_HOST=${MAIL_HOST}
      - MAIL_PORT=${MAIL_PORT}
      - MAIL_ENCRYPTION=${MAIL_ENCRYPTION}
      - MAIL_USERNAME=${MAIL_USERNAME}
      - MAIL_PASSWORD=${MAIL_PASSWORD}
      - MAIL_FROM_ADDRESS=${MAIL_FROM_ADDRESS}
      - MAIL_FROM_NAME=${MAIL_FROM_NAME}
      - MAKE_WEBHOOK_URL=${MAKE_WEBHOOK_URL}
      - MAKE_API_KEY=${MAKE_API_KEY}
      - MEILISEARCH_HOST=http://search:7700
      - MEILISEARCH_KEY=${MEILI_MASTER_KEY}
      - GALLERY_CLEANUP_GRACE_MONTHS=${GALLERY_CLEANUP_GRACE_MONTHS}
      - AUTH_THROTTLE_LIMIT=10
      - DOWNLOAD_THROTTLE=120
      - ZIP_DOWNLOAD_THROTTLE=10
      - CHECKOUT_THROTTLE_USER_PER_HOUR=${CHECKOUT_THROTTLE_USER_PER_HOUR:-5}
      - CHECKOUT_THROTTLE_IP_PER_HOUR=${CHECKOUT_THROTTLE_IP_PER_HOUR:-10}
      - CHECKOUT_THROTTLE_IP_PER_DAY=${CHECKOUT_THROTTLE_IP_PER_DAY:-30}
      - CHECKOUT_IDEMPOTENCY_TTL_MINUTES=${CHECKOUT_IDEMPOTENCY_TTL_MINUTES:-30}
      - TURNSTILE_SITE_KEY=${TURNSTILE_SITE_KEY:-}
      - TURNSTILE_SECRET=${TURNSTILE_SECRET:-}
      - TURNSTILE_ALLOWED_HOSTNAMES=${TURNSTILE_ALLOWED_HOSTNAMES:-}
      - TURNSTILE_USER_THRESHOLD_PER_HOUR=${TURNSTILE_USER_THRESHOLD_PER_HOUR:-3}
      - TURNSTILE_IP_THRESHOLD_PER_HOUR=${TURNSTILE_IP_THRESHOLD_PER_HOUR:-5}
      - TURNSTILE_FAILURE_USER_THRESHOLD_PER_HOUR=${TURNSTILE_FAILURE_USER_THRESHOLD_PER_HOUR:-3}
      - TURNSTILE_FAILURE_IP_THRESHOLD_PER_HOUR=${TURNSTILE_FAILURE_IP_THRESHOLD_PER_HOUR:-5}
      - TURNSTILE_FAILURE_WINDOW_SECONDS=${TURNSTILE_FAILURE_WINDOW_SECONDS:-3600}
      - FTP_STORAGE_PATH=/var/www/ftp
      - STRIPE_KEY=${STRIPE_KEY}
      - STRIPE_SECRET=${STRIPE_SECRET}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
      - STRIPE_CHECKOUT_NEW_ACCOUNT_HOURS=${STRIPE_CHECKOUT_NEW_ACCOUNT_HOURS:-24}
      - STRIPE_CHECKOUT_ENABLED=${STRIPE_CHECKOUT_ENABLED:-true}
      - STRIPE_STALE_PAYMENT_INTENT_HOURS=${STRIPE_STALE_PAYMENT_INTENT_HOURS:-2}
      - STRIPE_CUSTOMERS_ENABLED=${STRIPE_CUSTOMERS_ENABLED:-true}
      - PROXY_DELIVERY_HEADER=${PROXY_DELIVERY_HEADER}
      - PHOTO_STORAGE_PATH=${PHOTO_STORAGE_PATH}
      - AI_ENABLED=${AI_ENABLED}
      - AI_TYPE=${AI_TYPE}
      - AI_BASE_URL=${AI_BASE_URL}
      - AI_API_KEY=${AI_API_KEY}
      - AI_MODEL=${AI_MODEL}
      - AI_SESSION_HEADER=${AI_SESSION_HEADER}
      - AI_SESSION_PREFIX=${AI_SESSION_PREFIX}
    healthcheck:
      test: ["CMD-SHELL", "kill -0 1 2>/dev/null && grep -q php-fpm /proc/1/cmdline"]
      interval: "30s"
      timeout: 10s
      retries: 3
      start_period: 40s
    command: >
      sh -c "
      if [ \"$$(id -u)\" -ne 1000 ] || [ \"$$(id -g)\" -ne 1000 ]; then
        echo 'FATAL: Backend muss als UID:GID 1000:1000 laufen. Start verweigert (fail-closed)!';
        exit 1;
      fi;
      if [ -z \"$${APP_KEY}\" ] || [ -z \"$${JWT_SECRET}\" ] || [ -z \"$${FILE_ENCRYPTION_KEY}\" ] || [ -z \"$${ADMIN_EMAIL}\" ] || [ -z \"$${ADMIN_PASSWORD}\" ] || [ -z \"$${PHOTO_STORAGE_PATH}\" ]; then
        echo 'FATAL: APP_KEY, JWT_SECRET, FILE_ENCRYPTION_KEY, ADMIN_EMAIL, ADMIN_PASSWORD oder PHOTO_STORAGE_PATH ist nicht gesetzt. Start verweigert (fail-closed)!';
        exit 1;
      fi;
      case \"$${PHOTO_STORAGE_PATH}\" in
        /*) ;;
        *) echo 'FATAL: PHOTO_STORAGE_PATH muss ein absoluter Mount-Pfad sein. Start verweigert!'; exit 1;;
      esac;
      for path in /var/www/html \"$${PHOTO_STORAGE_PATH}\" /var/www/ftp; do
        if [ ! -d \"$${path}\" ] || [ ! -w \"$${path}\" ] || [ \"$$(stat -c '%u:%g' \"$${path}\")\" != '1000:1000' ]; then
          echo \"FATAL: Runtime-Pfad $$path fehlt, ist nicht schreibbar oder gehört nicht UID:GID 1000:1000. Start verweigert!\";
          exit 1;
        fi;
      done;
      php artisan cache:clear;
      php artisan optimize;
      php artisan migrate --force && php artisan db:seed --force && php artisan admin:update || exit 1;
      php artisan scout:sync-index-settings;
      php artisan queue:restart;
      echo 'Starte Hintergrund-Prozesse...';
      php artisan queue:work --tries=3 &
      (while true; do php artisan schedule:run >> /dev/null 2>&1; sleep 60; done) &
      echo 'Starte PHP-FPM...';
      exec php-fpm
      "
    networks:
      portal_internal:
      webnet:
        ipv4_address: 172.18.0.31

networks:
  webnet:
    external: true
  portal_internal:

volumes:
  db_data:
  search_data:
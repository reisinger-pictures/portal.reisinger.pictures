# Definition of Done
- Tests must exist
- Defined state in the `features/` folder (canonical indexes: `features/README.md` and `features/tech/README.md`) must match the implementation and be up to date.

If unsure what is correct, ask the user

**if met:** Delete items from Agents.todo.md file
**if not met:** Update the definition on what is still missing